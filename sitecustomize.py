# CVGO startup extension: registers the AI interview routes before Gunicorn serves the app.
import interview_api
# CVProfit launch hardening: trial duration, visible branding and CV date rendering.
import launchfix
