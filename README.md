# CVGO

Generador de CV y herramientas de candidatura con acceso PRO.

## Arranque local

```bash
pip install -r requirements.txt
python app.py
```

Configura las variables de entorno de Stripe y sesión usando `.env.example` como referencia.

> Nunca publiques claves secretas de Stripe ni las pongas en el navegador.
